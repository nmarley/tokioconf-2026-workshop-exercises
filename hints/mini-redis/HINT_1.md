Run the server and the bench tool with higher load, then stop the server
with Ctrl-C to see the runtime metrics. Look at the poll time histogram.
How long are the polls taking? Are any of them unusually long?
